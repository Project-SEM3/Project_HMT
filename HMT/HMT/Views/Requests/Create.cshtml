@model HMT.Models.HMTModel.RequestPostModel

@{
    ViewData["Title"] = "Create Request";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main class="p-6">
    <h1>@ViewBag.data</h1>

    <!-- Main change -->
    <section class="mb-6 rounded-md shadow bg-white w-full p-5 dark:bg-dark">
        <div class="flex items-center justify-between">
            <h3 class="text-lg font-medium dark:text-white mb-3">Request to buy stationery</h3>
            <button type="button"
                    class="addBtn py-2 px-5 rounded border border-primary font-medium hover:bg-gray-100 dark:bg-gray-700 dark:hover:bg-gray-600 dark:text-white"
                    id="requestStationeryBtn">
                <span><i class="fa-solid fa-plus"></i></span>
                Add stationery
            </button>
        </div>
        <form asp-action="CreateRequest" method="post" class="mt-5" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div id="manager"></div>
            <div id="requestStationery">
                @*<div class="grid grid-cols-5 gap-3 mb-6">
                    <div>
                        <label for="category" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
                            Select stationery
                        </label>
                        <select asp-for="@Model.category" id="category" 
                                class="category outline-none bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:border-primary block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500">
                            @foreach (var item in Model.Categories)
                            {
                                <option value="@item.Id">@item.NameCategory</option>
                            }
                        </select>
                    </div>
                    <div>
                        <label for="product" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
                            Select stationery type
                        </label>
                        <select asp-for="@Model.product" id="product"
                                class="product outline-none bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:border-primary block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500">
                            @foreach (var item in Model.Products)
                            {
                                <option value="@item.Id">@item.NameProduct</option>
                            }
                        </select>
                    </div>
                    <div>
                        <label for="" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Quantity</label>
                        <input asp-for="@Model.quantity" type="number"
                               class="bg-gray-50 border border-gray-300 text-gray-900 outline-none text-sm rounded-lg focus:border-primary block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
                               value="1" required min="1">
                    </div>
                    <div>
                        <label for="" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Note</label>
                        <input asp-for="@Model.note" type="text"
                               class="bg-gray-50 border border-gray-300 text-gray-900 outline-none text-sm rounded-lg focus:border-primary block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
                               placeholder="Vd: Thien Long Pen">
                    </div>
                    <div class="flex flex-col items-center">
                        <h5 class="block mb-2 text-sm font-medium text-gray-900 dark:text-white text-center">Action</h5>
                        <button type="button"
                                class="text-white rounded-md bg-rose-500 py-2 px-10 hover:bg-rose-700 deleteRequest">
                            Delete
                        </button>
                    </div>
                </div>*@
            </div>

            <div class="flex items-center justify-center mt-12">
                <button class="py-3 px-16 font-medium bg-primary hover:bg-sky-600 rounded-md text-white">
                    Request 
                    <span class="ml-2"><i class="fa-solid fa-up-right-from-square"></i></span>
                </button>
            </div>
        </form>
    </section>
    <!-- Main change end -->

</main>
